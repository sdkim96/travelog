var params = {
  container: document.querySelector("#lottie5"),
  renderer: "canvas",
  loop: true,
  autoplay: true,
  path: "static/json/loop-ex97.json",
};

var anim;

anim = lottie.loadAnimation(params);
